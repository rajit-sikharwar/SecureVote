// Included inside CandidateCard / ProgressBar. Reusing ProgressBar for this.
// Exporting an alias for convenience if needed, otherwise ProgressBar is fine.
import { ProgressBar } from '../ui/ProgressBar';
export const ResultBar = ProgressBar;
